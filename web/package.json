{
  "name": "gocommender-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Frontend for GoCommender AI music discovery",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^7.0.4"
  }
}
